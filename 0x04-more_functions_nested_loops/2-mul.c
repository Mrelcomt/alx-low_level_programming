#include <stdio.h>
/**
 * mul - used to multiply two numbers
 * @a: first num to multiply
 * @b: second num to multiply
 * Return: product of two numbers
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
