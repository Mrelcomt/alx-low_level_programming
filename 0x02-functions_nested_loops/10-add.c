#include "main.h"
/**
 * add - adds two numbers and return result
 * @a: the first number to add
 * @b: the second number to add
 *
 * Return: the sum of the numbers
 *
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
